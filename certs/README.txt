Coloque aqui los archivos del cliente:

  logo.png        - Logo de la institucion (recomendado: 200x80 px, fondo transparente PNG)
  firma.p12       - Certificado de firma electronica SRI (archivo .p12)

Estos archivos se montan como solo lectura en los contenedores.
No se incluyen en el repositorio git (ver .gitignore).
